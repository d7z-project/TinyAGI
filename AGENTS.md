# 当前阶段与设计推进约定

- 当前处于设计阶段。按 2026-09-20 最新用户要求，原 Q1–Q7 通过一手资料论证完成设计收尾，不再运行真实验证、模型调用或新增实验，不推进产品实现。设计机制已有资料支撑即可确定；实际性能、真人体验与具体接入参数作为适用边界，不自动保留为当前待办。以后仅在用户明确要求实验／实现或提出新问题时重新确定范围。
- 当前推进重点为完整认知系统与端到端任务：主体目标、注意力、持续活动、记忆、心智协作、能力调用、交付和复盘如何共同工作。按最新用户要求，不继续扩展锁、事务、备份、恢复等细节边界证明，也不把驱动选型或底层故障实验当作整体设计的前置条件。已有细节结论和实验保留，设计优先覆盖完整任务闭环。
- 按 2026-09-20 用户认可，统一事件交互贯穿外部观察、能力请求、结果、状态变化与运行控制。能力可创建或接入外部事件源，闹钟仅是通知源示例，不内置为核心业务模块。宿主负责来源绑定、订阅、关联、状态处理与按需认知接续；内部有目的的接续不必等待外部通知。统一事件不等于全量广播、每事件调用模型、全事件溯源或引入独立消息中间件。
- 当前主体采用形态无关的数字环境与通用设备能力抽象，不预设特定外形或硬件终点。TinyAGI 核心保持单机、单核心二进制、单宿主进程；允许宿主管理的本地能力子进程、原生构建产物及可选工具链依赖，额外程序不拥有第二份主体真值；业务状态使用嵌入式 SQLite，原始资源使用本地文件系统。按 2026-09-20 最新用户要求，向量检索通过公共能力 API 与 Go SDK 接入独立向量数据库，本地及远程均纳入设计；默认本地单节点，Qdrant 为当前默认选择。公共契约与厂商 SDK、部署位置分开，供后续扩展。具体产品与版本的验证状态以 `docs/engineering-reference.md` 和库接入参考为准。
- 删除 TinyAGI 分布式运行、Kubernetes、平台控制与设备插件路线，不继续设计节点接管、集群复制、服务发现或对象存储后端。旧讨论与实验保留为历史证据，不恢复为当前要求。远程向量服务与外部模型／工具 API 是能力接入，不是部署 TinyAGI 集群；其网络、处理范围与服务契约仍须明确。
- 按最新用户要求，设计文档只维护逻辑设计：`DESIGN.md` 与六份设计专题定义职责、业务身份、状态所有权、交互及行为约束，逻辑图不混入语言、库、SDK、数据库产品、进程或部署位置。物理部署、软件依赖和技术选型集中维护于 `docs/engineering-reference.md`，go-mini 库行为另见其接入参考；通过导航关联，不复制回设计正文。向量索引仍为派生数据，不替代业务状态、原始证据或推理 KV；执行中断、并发结果和外部动作不确定性仍须有逻辑约束。
- Web 管理工作台方案已获用户认可，覆盖所有受管理业务对象与可配置行为。逻辑设计在 `docs/management-workspace.md`，技术承载在工程参考；查询／管理请求沿现有状态所有者处理，管理身份与聊天身份分开，展示配置来源、作用范围、生效时机与实际结果。确定性管理不依赖模型；浏览资源不自动注入认知，真实与沙箱范围分别装配。不把该认可解释为进入实现或授权运行实验。
- 管理范围包括认知之外的模型资产、推理服务、资源及其维护。Secret 使用特殊投影：模型仅见获准的引用、用途与状态，普通用户可经专用指令及一次性页面等可信输入维护自己的机密，归属、维护权和使用权分别定义，管理面板可按权限调整。确定性管线负责分流、校验转换、保管投影、授权注入及返回过滤；原值和入口访问凭证绕过模型，由受信宿主按用途绑定使用，不进入聊天、认知、普通事件正文、日志、索引或沙箱副本。具体机制维护在工作台专题与运行协议，技术承载在工程参考；本轮仍仅做设计。
- `DESIGN.md` 是当前设计的统一入口。每轮推进前，先将上一轮逻辑结论写入其中，工程结论写入独立工程参考：方案状态、选择理由、实验证据与边界、被否决或暂缓的替代项及原因、开放问题。
- 总设计必须自顶向下展开：目标和边界 → 总体架构与职责 → 核心概念和跨系统流程 → 子系统与机制 → 选型依据和验证。保留阅读导航、稳定章节锚点、职责索引与历史主题对应，不能让最近的实验记录替代整体架构。
- 设计变更同时检查总图、概念关系、运行场景和相关细节的一致性；更改职责或交互边界时，先更新上层视图再补机制。子文档用于深入阅读，`DESIGN.md` 本身仍须呈现完整系统及主要选择理由。
- 当前流程为：先回写设计，再核对一手资料，明确采用方案、理由、备选及适用边界，同步专题与收尾台账。仅在用户以后明确要求实验时，才固定实验问题、对照、条件与指标并运行；原始数据和失败须保留，结论回写总稿。
- 区分用户目标、已确认库行为、实测结果、工程判断和未验证假设。模拟结果不能冒充真实模型效果或硬件性能；未测方案不能写成被实验证伪。
- 对当前无法验证的相关问题，查阅官方文档、源码、厂商契约或原论文，记录日期、适用版本、支持的命题、设计推论和适用边界。资料论证不能冒充本项目的端到端可靠性、认知质量或性能实测。
- `GPT.md` 是体量较大的历史讨论，优先从 `DESIGN.md` 和专题文档恢复进度，仅按需定位历史。相邻 `../go-mini` 用于核对其现有能力，未经任务要求不修改该仓库。
- 文档入口为 `README.md`，完整资料和术语索引为 `docs/README.md`。总设计保存完整架构与关键取舍，专题维护详细协议，实验报告保存完整统计；避免将同一实验数字和阶段回顾重复粘贴到各处。长专题保留目录、稳定锚点及返回入口。按用户要求，所有文档内容直接展示，不使用折叠块，长文通过标题、目录及链接组织。已记录哈希的实验预登记、脚本与原始数据按证据保存，整理文档时不改写它们。

- 文档按主题和日期维护，不设置 TinyAGI 产品式版本号。状态唯一维护于 `docs/research-plan.md`；副本生命周期归沙箱专题，页面操作归管理专题，库读取记录归 go-mini 接入参考。清理重复进度叙述，历史写稿号、冻结实验修订和第三方版本不得冒充当前设计版本，也不得为消除字样改写原始证据。

- 按 2026-09-20 用户认可，热加载采用受约束模块、类型推导、少量结构化注释及自动生成适配；长期业务状态由宿主管理，受管理函数以稳定标识调用，跨调用不保留旧实现闭包，一次执行使用确定的代码与普通行为配置，授权、机密资格、预算及控制按当前状态核对。调用描述、表单及蓝图随来源修订一致切换，声明不等于授权，机密保持引用。编写／调用契约归运行专题，编辑生效归工作台，语法及生成工具归工程参考，库事实归接入参考；认可仅进入设计，不授权实现或实验。

- 按 2026-09-20 用户认可，能力扩展采用“发现／复用／组合 → 接口与候选 → 构建 → 分层试验 → 范围化采用 → 调用与复盘”。最大化复用 go-mini MRPC 的 Go／Mini-Go／Rust／TypeScript 生成、本地／跨进程绑定、资源和服务替换；Go／Rust 原生及 Node.js（npm）能力以受管理程序接入，核心宿主更新仍属软件更新。接口与实现身份分开，跨语言声明单一来源，提交生效包含实际调用绑定切换，旧调用／资源保留归属。局部 VM、能力组合与 AGI 副本分层，构建全过程受控，子工作共享父活动预算，机密和资源保持既有投影规则。库已有 API 与宿主待封装的子进程／子 VM 服务分别记录；逻辑契约在运行专题、试验在沙箱、管理在工作台，工具链和进程在工程参考，库事实在接入参考。当前只维护设计，不实施或运行新实验。

- 按 2026-09-20 用户认可，主动学习与自我迭代是正式存在、可启用的实验性扩展：兴趣、成长方向、能力盲区和环境变化可形成自主学习活动，复用既有目标、活动、资源、构建及沙箱；允许比较学习策略自身的候选，研究价值与正式采用分开。学习强度由宿主限制，最低可关闭，不自主发起／继续学习或自动采用待用候选；在途只做停止、结果登记及必要清理，实际状态可见。普通任务、正常记忆记录和已有能力使用保留，用户明确学习任务按独立范围接纳且不暗中开启主动学习。学习扩展及副本不能提高强度、刷新父预算或改写当前评价；具体策略、初始档位与额度按用途配置，不假定无限增长。策略在主体专题，强度在运行协议，评价在沙箱，管理在工作台，技术接入与库事实分开维护；本轮仅更新设计，不授权实施或实验。

- 按 2026-09-21 用户认可，人物以当前完整初始化提示词形成个体实现，后续以相邻差异及语义迁移说明引导 Self 按实际程序自迁移，接受不同模型／人格的差异，保持共同运行契约和稳定身份。宿主提供独立于个体程序的有界初始化／修复引导和固定维护入口；API 弃用注释须配套真实兼容实现，按若干稳定接口修订保留，具体次数由支持策略确定，不能以旧缓存承诺跨客户端可启动。迁移保留历史、关系和配置，候选检查后采用；获准的必要兼容维护使用独立范围与预算，学习关闭时仍可执行，不开启主动学习或扩大权限。逻辑在总设计／主体／运行专题，操作在工作台，评价在沙箱，技术承载及库读取记录分开；当前只写入设计，不生成运行提示词、实施或运行实验。

- 按 2026-09-21 用户认可，客户端预制能力库随核心客户端交付与更新，权威定义、实现及资料对 Self 只读，由客户端维护；Self 可按需发现、调用、组合、独立包装或不使用。维护权、调用权限及读取／计算／写入／外部动作等实际作用分别定义，状态、Secret、预算和事件仍沿原契约。初始化引导、正常主体与管理共用同源契约并按身份过滤，资源保持投影。新增能力可选，已依赖实现更新承担兼容责任并与 Self 自迁移衔接；不统一人格程序。逻辑归运行协议，交付装配归工程参考，库事实另存，管理及评价同步；只写入最终认可方案，不记录被纠正的中间解释。本轮仅维护文档，不实施或实验。

- 按 2026-09-21 用户认可，核心与任务解耦：Self、业务任务、认知执行和外部操作生命周期分开；长工作接纳后独立管理，任务等待、超时、失败或中止不关闭 Self、不长期占用唯一认知入口，仍计入原任务及全局预算。控制台、获授权用户及未来安全审计模块经可信宿主可直接打断执行、暂时阻断或中止任务，不等待认知批准；审计建议与获授权决定分开。控制覆盖后续准入、子工作、在途操作和迟到结果，实际效果及收尾责任保留；多原因解除须有权限，不能换标识／实例规避有效限制，无关任务及共享资源保持。控制生效与实际停止分别展示。逻辑归主体／运行专题，操作归工作台，评价归沙箱，执行装配归工程参考，mini-go 固定源码／测试阅读与实测分别记录；仅写最终方案，不实施或新增实验。

- 按 2026-09-21 用户认可的整体审计结论，Episode／Task／Operation 及执行尝试分层，控制携带明确类型和传播范围；认知中断不自动取消独立接纳工作。步骤内操作独立接纳，最终提案引用既有操作，不重复派发或抹去成本／效果。Mind 私有判断、活动委托和 Self 公共整合分开；代码及普通配置随执行固定，授权、Secret、额度、学习准入和控制按当前状态核对。个体状态由宿主管归属、结构及迁移，功能采用不等于实现信任，学习关闭保留必要任务内工具构建。详细契约及确认范围唯一见 `docs/research-plan.md#architecture-consistency` 与关联专题；完整副本重建细则仍保留原建议状态。本次仅维护文档，不实施或运行实验。

- 按 2026-09-21 用户补充，外部能力实现支持 Go／Rust／Node.js（npm）。Node 复用 go-mini 的 TypeScript ESM 生成与独立 RPC SDK，程序包、依赖和运行环境关联固定实现；继续遵守任务解耦、受控准备／执行、Secret 投影、实现信任及绑定切换。具体 Node／npm／Worker／WASM 与传输事实在工程参考和 go-mini 接入记录维护，不将语言与部署细节加入逻辑总图；本次只修订设计，不安装依赖或运行实验。

- 按最新用户要求维护文档发布：原始 `experiments/`、本地 `GPT.md`、凭据与 IDE 文件不加入 Git 或 mdBook；`docs/experiments/` 的整理报告可公开。`SUMMARY.md` 定义公开章节，书稿只从清单生成；保留本地证据，公开构建转换未发布材料和相邻源码引用，敏感扫描不得输出原值。允许执行文档构建、链接与 Mermaid 检查，不因此运行认知实验或产品实现。入口为 `docs/documentation-publishing.md`。
